db {
  driverClassName = "org.sqlite.JDBC"
  dataSource {
    url = "jdbc:sqlite:./app-data/db/leetcodequiz.sqlite"
    user = ""
    password = ""
  }
}

test-db {
  driverClassName = "org.sqlite.JDBC"
  dataSource {
    url = "jdbc:sqlite:file:leetcodequiz-test?mode=memory&cache=shared"
    user = ""
    password = ""
  }
}
